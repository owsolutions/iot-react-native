/**
 * This is a theme file, which describes the ui elements
 * look and feel normally.
 */

import { StyleSheet } from 'react-native';

export default theme = {
    colors: {
        default: 'blue'
    }
};
